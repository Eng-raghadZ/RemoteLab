// src/pages/Dashboard.jsx
//
// The "/dashboard" route (see App.jsx — routing is untouched) now
// renders the File Upload Page. Kept as a thin re-export so App.jsx's
// import of "./pages/Dashboard" doesn't need to change.

export { default } from "./UploadPage";

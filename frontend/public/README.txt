Place background4k.png here (public/background4k.png).
Vite serves everything in /public/ from the site root, so the app
references it as /background4k.png — see BG_IMAGE_URL in
src/pages/RemoteLabLanding.jsx.
